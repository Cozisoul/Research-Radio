module.exports = {
  root: true,
  env: { node: true },
  extends: [
    'eslint:recommended',
  ],
  parserOptions: {
    ecmaVersion: 'latest',
  },
  rules: {
    // Add any specific rules for your config files here if needed
  },
};